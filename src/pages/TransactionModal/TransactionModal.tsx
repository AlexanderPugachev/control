import React from 'react';
import { Modal, Text } from '../../components';

export const TransactionModal: React.FC = () => {
  return (
    <div>
      <Modal>
        <Text>hello everywhere</Text>
      </Modal>
    </div>
  );
};